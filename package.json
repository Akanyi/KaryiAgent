{
  "name": "karyi-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A powerful AI-powered terminal assistant with security and auditing capabilities",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present"
  },
  "keywords": [
    "ai",
    "terminal",
    "assistant",
    "cli",
    "agent"
  ],
  "author": "Karyi Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
